# FN软仓应用上架助手 使用说明书

**版本：v1.5**

适用于 FN软仓服务端搭建者（自托管源 / 官方源管理员 / 三方源开发者）


## 一、简介

FN软仓应用上架助手是一个 Windows 桌面工具，用于将 FPK 应用包快速上架到 FN软仓服务端。

**解决的核心问题：**

| 传统方式 | 使用本工具 |
|---------|----------|
| 手动解压 FPK 查看 manifest | 自动解析 |
| 手动复制文件到 Gitee 仓库 | 自动复制 + Git 推送 |
| 手动复制文件到飞牛 data/apps/ | 自动复制（通过 SMB） |
| 手动编辑 fn-appstores.json | 自动更新 |
| 手动调用 /api/refresh | 自动刷新缓存 |
| 逐个处理多个 FPK | 批量处理 |
| 手动编辑 notice.json | 公告管理界面（v1.5 新增） |
| 手动输入分类标签 | 固定下拉选择（v1.5 新增） |


## 二、两种部署模式

工具支持两种部署模式，根据你的配置自动切换：

### 模式一：飞牛本地模式（纯自托管）

FPK 存放在飞牛本地 `data/apps/`，下载流量走飞牛家庭带宽。

**适用场景：**
- 家庭宽带上行够用
- 不想依赖 Gitee/GitHub
- 应用包体积较大（>50MB）
- 用户量小

**配置要求：**
- 飞牛 data 本地映射路径（必填）
- 飞牛服务端地址（必填）
- Gitee 配置（留空）

### 模式二：Gitee 分流模式（混合托管）

小文件走 Gitee CDN，大文件走飞牛本地。

**适用场景：**
- 家庭宽带有限
- 大部分应用包小于 10MB
- 希望降低飞牛带宽压力

**配置要求：**
- 飞牛 data 本地映射路径（必填）
- 飞牛服务端地址（必填）
- Gitee 仓库本地映射路径（必填）
- Gitee Raw 地址（必填）
- 本地阈值（建议 10）


## 三、环境要求

### 3.1 硬件/系统

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / Windows 11 |
| Python | Python 3.8+（或使用打包后的 exe） |
| 网络 | 能访问飞牛 NAS（内网或 VPN） |
| 飞牛 | FN软仓服务端已部署并运行 |

### 3.2 飞牛服务端要求

| 项目 | 要求 |
|------|------|
| 服务端版本 | **v2.5.0 或更高**（v1.5 工具要求） |
| 服务端运行状态 | 正常运行，端口可访问 |
| data 目录 | 可读写（通过 SMB 挂载） |

### 3.3 Gitee 模式额外要求

| 项目 | 要求 |
|------|------|
| Gitee 仓库 | 已创建且为公开仓库 |
| 本地仓库 | 已克隆到 Windows 本地 |
| Git | 已安装并配置好认证 |


## 四、安装与启动

1. 下载 `FN软仓应用上架助手.exe`
2. 双击运行（无需安装 Python）


## 五、配置说明

### 5.1 配置项详解

| 配置项 | 必填 | 说明 | 示例 |
|--------|------|------|------|
| **飞牛 data 本地映射路径** | ✅ 必填 | 飞牛 FN软仓服务端的 data 挂载目录，通过 SMB 映射到 Windows 的路径 | `\\192.168.3.20\docker\fn-appstores-server\data` |
| **飞牛服务端地址** | ✅ 必填 | 飞牛 FN软仓服务端的访问地址 | `http://192.168.3.20:5660` |
| **Gitee 仓库本地映射路径** | ⬜ 可选 | Gitee 仓库在 Windows 本地的路径（仅 Gitee 模式需要） | `D:\应用开发\gitee\fn-appstores` |
| **Gitee Raw 地址** | ⬜ 可选 | Gitee 仓库的 raw 访问地址（仅 Gitee 模式需要） | `https://gitee.com/用户名/仓库名/raw/master/server-data` |
| **本地阈值 (MB)** | ⬜ 可选 | 小于此值走 Gitee，大于等于走飞牛本地。填 0 或负数则全部走飞牛本地 | `10` |

### 5.2 如何获取飞牛 data 本地映射路径

在 Windows 文件管理器地址栏输入：
```
\\飞牛IP\docker\fn-appstores-server\data
```
例如：
```
\\192.168.3.xx\docker\fn-appstores-server\data
```

如果提示需要凭证，输入飞牛的登录账号和密码。

### 5.3 如何获取 Gitee Raw 地址

格式：
```
https://gitee.com/你的用户名/仓库名/raw/分支名/server-data
```

示例：
```
https://gitee.com/xxxx/fn-appstores/raw/master/server-data
```


## 六、固定标签分类（v1.5 新增）

v1.5 版本中，应用分类（Tags）不再支持自由输入，改为从客户端固定的 12 个分类中下拉选择。

**支持的分类列表：**

| 分类 | 说明 | 分类 | 说明 |
|------|------|------|------|
| 影音 | 音视频播放/处理 | 办公 | 办公效率工具 |
| 下载 | 下载管理器 | AI | AI 相关工具 |
| 设计 | 设计/图像处理 | 社交 | 社交/通讯 |
| 网络 | 网络工具 | 工具 | 实用工具 |
| 生活 | 生活助手 | 效率 | 效率工具 |
| 开发 | 开发工具 | 游戏 | 游戏/娱乐 |

**使用方式：**

在「FPK 文件列表」区域上方，选择「应用分类 (Tags)」下拉框，所有被上架的应用将统一使用该分类标签。


## 七、公告管理（v1.5 新增）

v1.5 版本新增公告管理功能，支持在线编辑服务端的 `notice.json`（v2.5.0 格式）。

### 7.1 打开公告管理

点击主界面「📋 公告管理」按钮，弹出公告管理对话框。

### 7.2 管理轮播图

- **添加**：点击「添加」，输入图片 URL
- **编辑**：选中轮播图，点击「编辑」，修改 URL
- **删除**：选中轮播图，点击「删除」

### 7.3 管理公告记录

- **添加**：点击「添加」，填写标题、日期、内容（支持 HTML）
- **编辑**：选中公告，点击「编辑」，修改内容
- **删除**：选中公告，点击「删除」

### 7.4 公告启用/禁用

勾选或取消「启用公告」开关，控制公告是否在前端显示。


## 八、使用流程

### 8.1 首次使用

1. **填写配置**
   - 必填：飞牛 data 本地映射路径、飞牛服务端地址
   - 可选：Gitee 仓库本地映射路径、Gitee Raw 地址、本地阈值

2. **选择应用分类**
   - 在「应用分类 (Tags)」下拉框中选择合适分类

3. **点击「保存方案」**
   - 输入方案名称，配置会自动保存

### 8.2 上架应用

1. **添加 FPK 文件**
   - 点击「添加文件」选择单个或多个 FPK
   - 点击「添加文件夹」自动扫描文件夹内所有 FPK
   - 支持拖拽 FPK 文件到窗口

2. **确认分类标签**（v1.5 新增）
   - 上架前确认「应用分类 (Tags)」下拉框已选择正确分类

3. **点击「开始上架」**

4. **查看运行日志**
   - 日志区会显示每个文件的处理进度
   - 进度条显示整体完成情况

5. **完成验证**
   - 在 FN软仓客户端点击「刷新」
   - 新应用应出现在列表

### 8.3 管理公告（v1.5 新增）

点击「📋 公告管理」按钮，在对话框中编辑轮播图和公告记录，保存即生效。


## 九、云服务器用户特别说明

### 9.1 场景说明

如果你使用云服务器（如阿里云 ECS、腾讯云 CVM）部署 FN软仓服务端，而不是在飞牛 NAS 上运行，操作方式略有不同。

```
┌─────────────────────────────────────────────────────┐
│  你的场景：服务端在云服务器，工具在本地电脑运行      │
├─────────────────────────────────────────────────────┤
│  本地电脑 (Windows)                                │
│  ├── FN上架助手.exe                                │
│  ├── Gitee仓库 (D:\fn-appstores\)                 │
│  │   └── 通过SMB或SFTP挂载云服务器data目录        │
│  └── 运行上架助手 → 自动更新云服务器data           │
│                                                    │
│  云服务器                                          │
│  ├── FN软仓服务端 Docker容器                       │
│  ├── data/ 目录                                   │
│  │   ├── fn-appstores.json                        │
│  │   └── apps/                                   │
│  └── 服务端地址: http://公网IP:5660              │
└─────────────────────────────────────────────────────┘
```

### 9.2 云服务器 data 目录的访问方式

| 方式 | 说明 | 推荐度 |
|------|------|--------|
| **SMB 挂载** | 在云服务器上安装 Samba，将 data 目录共享出来，Windows 直接映射为网络驱动器 | ⭐⭐⭐⭐⭐ |
| **SFTP 映射** | 使用 SFTP 工具（如 WinSCP）将云服务器目录映射为本地盘 | ⭐⭐⭐ |

### 9.3 推荐方式：SMB 挂载

**在云服务器上配置 SMB 共享：**

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install samba samba-common-bin
sudo smbpasswd -a 你的用户名

# 编辑 /etc/samba/smb.conf，添加：
[fn-data]
   path = /path/to/fn-appstores-server/data
   browseable = yes
   read only = no
   guest ok = no
   valid users = 你的用户名

sudo systemctl restart smbd
```

**在 Windows 上映射网络驱动器：**
- 地址：`\\云服务器IP\fn-data`
- 凭证：云服务器的 SMB 用户名和密码

### 9.4 云服务器场景配置示例

```
配置 (飞牛必填，Gitee可选)
├── 飞牛 data 本地映射路径: \\123.45.67.89\fn-data
├── 飞牛服务端地址: http://123.45.67.89:5660
├── Gitee 仓库本地映射路径: D:\应用开发\gitee\fn-appstores
├── Gitee Raw 地址: https://gitee.com/hhxs2025/fn-appstores/raw/master/server-data
└── 本地阈值 (MB): 10
```

### 9.5 注意事项

| 注意点 | 说明 |
|--------|------|
| **安全组/防火墙** | 云服务器需开放 5660 端口（FN软仓服务端）和 445 端口（SMB，建议限制来源 IP） |
| **SMB 安全** | 建议仅允许你的本地电脑 IP 访问 SMB，避免暴露到公网 |
| **云服务器带宽** | 如果走飞牛本地模式，FPK 下载流量会消耗云服务器出站带宽（需付费） |
| **SMB 性能** | SMB 挂载受网络延迟影响，批量处理大量文件时可能稍慢 |


## 十、目录结构说明

### 10.1 使用本工具后，你的 Gitee 仓库结构

```
fn-appstores/                         # Gitee 仓库根目录
└── server-data/
    ├── apps/                         # FPK 文件 (工具自动复制)
    │   ├── dockfn-0.1.0.fpk
    │   └── file-tools-0.1.53.fpk
    ├── icons/                        # 应用图标 (工具自动提取)
    │   ├── dockfn.PNG
    │   └── file-tools.PNG
    └── previews/                     # 应用截图 (手动维护)
```

### 10.2 飞牛服务端 data 目录结构

```
data/                                 # 飞牛服务端挂载目录
├── fn-appstores.json                 # 应用清单 (工具自动更新)
├── apps/                             # FPK 文件 (大文件走这里)
│   └── file-collector-2.4.0.fpk
├── icons/                            # 图标 (大文件走这里)
│   └── file-collector.PNG
├── previews/                         # 截图
├── notice.json                       # 公告 (工具支持在线编辑)
└── stats.db                          # 下载统计
```


## 十一、自动分流逻辑

工具根据配置自动决定每个 FPK 的去向：

```
┌─────────────────────────────────────────────────┐
│                  开始处理 FPK                    │
└─────────────────────┬───────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────┐
│         检查 Gitee 配置是否完整？                │
└─────────────────────┬───────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │ 否                        │ 是
        ▼                           ▼
┌───────────────┐       ┌───────────────────────┐
│ 走飞牛本地     │       │   检查阈值是否 > 0？   │
│ (全部)        │       └───────────┬───────────┘
└───────────────┘                   │
                        ┌───────────┴───────────┐
                        │ 否                    │ 是
                        ▼                       ▼
                ┌───────────────┐       ┌───────────────┐
                │ 走飞牛本地     │       │ 文件 < 阈值？  │
                │ (全部)        │       └───────┬───────┘
                └───────────────┘               │
                                    ┌───────────┴───────────┐
                                    │ 是                    │ 否
                                    ▼                       ▼
                            ┌───────────────┐       ┌───────────────┐
                            │ 走 Gitee      │       │ 走飞牛本地     │
                            └───────────────┘       └───────────────┘
```


## 十二、常见问题

### Q1：飞牛 data 目录连不上？

1. 确认飞牛已开启 SMB 共享
2. 确认飞牛 IP 地址正确
3. 确认飞牛共享路径正确
4. 在 Windows 文件管理器尝试 `\\飞牛IP\docker\fn-appstores-server\data`

### Q2：Gitee 推送失败？

1. 确认 Git 已安装：`git --version`
2. 确认 Gitee 仓库路径正确
3. 确认已配置 Gitee 认证：
   ```bash
   cd D:\应用开发\gitee\fn-appstores
   git push
   ```
   手动推送一次确认认证通过

### Q3：发行版应用被自动跳过？

工具检测到 `server-data/icons-re/` 目录下已有同名图标，认为这是发行版应用（FPK 存放在其他开发者仓库），自动跳过，需手动处理。

### Q4：客户端看不到新上架的应用？

1. 确认服务端日志无错误
2. 在客户端「源管理」点击「刷新」
3. 检查 `fn-appstores.json` 是否已更新
4. 浏览器访问 `http://飞牛IP:5660/api/apps` 查看返回数据

### Q5：多个 FPK 批量处理会卡吗？

不会。工具使用独立工作线程处理，界面保持响应，进度条实时更新。

### Q6：能不能只配置飞牛，不配 Gitee？

可以。Gitee 配置全部留空，自动切换为「仅飞牛本地模式」，所有 FPK 存到飞牛 `data/apps/`。

### Q7：分类标签为什么只能下拉选择？不能自己输入？

v1.5 版本起，客户端 Tags 分类固定为 12 个（影音、办公、下载、AI、设计、社交、网络、工具、生活、效率、开发、游戏），为保持一致性，工具改为下拉选择。

### Q8：公告管理保存后多久生效？

保存后立即生效，无需重启服务端。客户端刷新后即可看到最新公告。

### Q9：公告内容支持什么格式？

支持 HTML 格式，可使用 `<p>`、`<b>`、`<ul>`、`<li>` 等标签。


## 十三、版本更新记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.5 | 2026-08-26 | 适配服务端 v2.5.0；新增固定标签分类下拉选择；新增公告管理功能（轮播图 + 公告记录）；上架时自动添加 `updated_at` 字段 |
| v1.4 | 2026-08-25 | 新增配置管理功能（多方案存储、一键切换）；优化界面标签名称 |
| v1.3 | 2026-08-25 | 支持批量处理，新增进度条，优化多文件处理性能 |
| v1.2 | 2026-08-25 | 支持根据大小自动分流，阈值可配置 |
| v1.1 | 2026-08-24 | 新增 Gitee 模式，支持图标自动提取 |
| v1.0 | 2026-08-23 | 初始版本，支持基础上架功能 |
